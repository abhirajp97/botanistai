# Required for the default model (free tier — get yours at console.groq.com)
GROQ_API_KEY=your_groq_api_key_here

# Optional: Users can also bring their own keys via the UI for other providers
# (Google Gemini, Anthropic Claude, OpenAI GPT-4o)
